#include <stdio.h>
/*this is the directive*/


int main(void){
    int height, length, width, volume, weight;


    printf("enter the height of the box: ", length, width, height);
    scanf("%d", &height);

    printf("total height: %d", height);

    return 0;
}
