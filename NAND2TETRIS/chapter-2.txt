BOOLEAN ARITHMETIC

ALU (core of CPU)

general purpose computer systems have the following features

- ADDITION (+)
- SIGN CONVERSION (pos to neg)
- SUBTRACTION (-)
- COMPARISON (=)
- MULTIPLICATION (X)
- DIVISION (/)

why addiiton is the 'master' key

- subtraction can be done in terms pf addition
- multiplication too
- division is repeated suptraction
- comparison is also subtraction

=====================

binary numbers
word size -> the 'slot'
max nonneg intger = 0 - 2^n - 1
8bit word size -> 2^8 - 1 = 255 posint

======================

binary addition
if last addition at the MSB leaves a carry bit -> leave it
a carry is always exactly 1, like in decimal

twos complement
convert to negatie -> inverse all bits, add 1
we sacrifice the MSB (2^n-1) vs (2^n)

3 -> 0011
-3 -> 1100 -> 1101 -> -8 + 4 + 1 = -8+5 = -3

negative gets more -(2^n-1), positive starts from 0 up to 2^n - 1


======================
ADDERS

half adder -> add 2 bits
full adder -> add 3 bits
adder -> add two n-bit numbers

ALU does a few things
- zero the input
- negate the input
- func - basic addition & bitwise op
- no knob (negate output)

word size -> 16bit

control bit ->  number of switches used to tell the factory
what to do to those number. essentualy a function/instruction in binary

example: 001110
zx = 0
nx = 0
zy = 1 -> 0
ny = 1  -> becomes 111111... (-1)
f = 1 -> add it -> x + (-1)
no = 0 -> dont invert the result