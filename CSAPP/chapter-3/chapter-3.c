#include <stdio.h>
#include <stdbool.h>

/*

program counter %eip -> the finger pointing
register -> holds 8 folders. those folder holds 32bit can read/change instantly
condition code register -> last answer
program memory -> massive warehouse



*/

bool float_ge(float x, float_y){
    unsigned ux = f2u(x);
    unsigned uy = f2u(y);

    //numbers have different signs
    //if x is positive then the statement is true

    // same sign
    // compare both which one iis larger

    // both numbers are negative



int main(void){
    return 0;
}
