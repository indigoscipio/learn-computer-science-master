#lang racket

;4.7 Write a procedure that computes the absolute value of its argument
;by finding the square root of the square of the argument
(define (abs n)
  (sqrt (sqr n))
  )


;4.8
; scientific notation
(define (scientific base exp)
  (* base (expt 10 exp))
  )
(scientific 7 -3) ; should return 7000

;(A harder problem for hotshots: Can you write procedures that go in the other direction? So you'd have
; sci-coefficient 7000 -> 7, sci-exponent 7000 -> 3

; helper function for changing log base
(define (tobase10 n)
  (/ (log n) (log 10))
  )

(define (sci-exponent n)
  (floor (tobase10 n))
  )

(define (sci-coefficient n)
  (/ n (- 10 (sci-exponent n)))
  )


; 4.9
; define proc discount that takes 2 args
; an items initial price and precentage discount
; it should return new price

(define (discount base-price percentage-cut)
  ; subtraction way
  ;(- base-price  (* (* percentage-cut 0.01) base-price))

  ; percentage way
  (* base-price (- 1 (* percentage-cut 0.01)) )
  )

;4.10
; write procedureto compute tip
; it should take total bill as arg and return the amount of tip
; it should tip by 15%, but it should know to round up so the total amount of money you leave
; (tip plus original bill) is a whole number of dollars.

; Number -> Number
; given the total billl, return how much it should tip (15% of bill)
(define (tip bill)
  (- (ceiling (+ (* 0.15 bill) bill)) bill)
  )

(tip 19.98) ; return 3,02
(tip 29.23) ;return 4.77
(tip 7.54) ;return 1.46


(define (hey n)
  'n
  )
(hey 456)