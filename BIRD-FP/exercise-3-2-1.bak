#lang racket

;3.1.1
; give example of expression which contains two occurences
; of theempty list
; the 1st occurence having the type [num]
; and second type [char]

;answer
; list1 = [[1,2,3],[]]
; list2 = [['a','b'],[]]

;3.1.2
; determine the number of elements of [a...b] and [a,b..c] in terms of a,b, and c
; answer:
; [a...b] -> simple range, starts with a and ends with b with +1 step, and a < b
; a, a+1, a+2, ... , b
; b - a + 1
; [a,b...c] -> stepped range with pattern
; c-a/b-a + 1