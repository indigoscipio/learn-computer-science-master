#lang racket

; if its toggled even times, its on,
; if its toggled odd times, its off
; all bulbs starts at off
; perfect square have odd number of factor

(define (main n)
  (integer? (sqrt n))
  )
(main '(3 6241 8191 0))
